class Solution:
    #find the longest palindrome in a string
    def longestPalindrome(self, s: str) -> str:
        main_map={}
        #edge case 1: When string is empty
        if s=="":
            return ""
        #edge case 2: When string is of length 1
        if len(s)==1:
            return s
        #Main logic
        for each_string in s:  
            return NotImplemented
        
